<div class="header">
	<h1><%= link_to "Short It ", root_path , :id => "head_text" %></h1> 
</div>
<div id="lgmn">
	<ul>
	<% if admin_signed_in? %>
	        <li>
	          <%= link_to('Logout', destroy_admin_session_path, :method => :delete) %>   
	          <%= current_admin.email %>
	        </li>
	        <li>
	          <%= link_to "Users", users_path %> 
	          <%= link_to "shorts", shorts_path %> 
	        </li>
	<% end %>
	</ul>
</div>